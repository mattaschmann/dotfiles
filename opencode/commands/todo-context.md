---
description: Do an atomic task that focuses on context around @Agent tags
---

- Grep for all `@Agent` tags in the codebase, respecting .gitignore.
- Form a context of the problem to solve based on the combination of all `@Agent`
  tags. Read the whole file and any related files as necessary.
- Verify with the user what the problem and suggested solution is before
  execution.
- Remove the `@Agent` comment lines once done.
